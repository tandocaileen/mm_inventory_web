// This file is kept for potential future use with StackedView
// Currently the CustomTable uses StatefulWidget for better performance with large datasets

import 'package:stacked/stacked.dart';

class CustomTableModel extends BaseViewModel {
  // Add any shared logic here if needed in the future
}
